num = int(input("Introduzca un número par: ")) #Pedir al usuario el número
matriz = 0 #Crear la lista vacía
if num%2 == 0: #Verificar que el número sea par
  for i in range(num): #Recorrer las filas de la matriz
    sample_listi=num
    #Agregar una sublista por cada fila
    sample_listi.append()
    for j in range(num): #Recorrer las columnas de la matriz
        sample_listj=num
        #Agregar el indice de la columna al elemento de la sublista correspondiente a esa fila 
        sample_listj.append()
print(sample_list)